<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Authentication - Vermicomposting System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .auth-card {
            transition: transform 0.2s;
        }
        .auth-card:hover {
            transform: translateY(-5px);
        }
    </style>
</head>
<body class="bg-light">
    <div class="container">
        <div class="row justify-content-center mt-5">
            <div class="col-md-8 text-center">
                <h1 class="mb-4">Vermicomposting System</h1>
                <h3 class="text-muted mb-5">Authentication Portal</h3>
            </div>
        </div>
        
        <div class="row justify-content-center g-4">
            <!-- Login Card -->
            <div class="col-md-4">
                <div class="card shadow auth-card h-100">
                    <div class="card-body text-center d-flex flex-column">
                        <h4 class="card-title mb-4">Login</h4>
                        <p class="card-text flex-grow-1">Already have an account? Sign in to access your dashboard.</p>
                        <a href="/auth/login" class="btn btn-primary">Login</a>
                    </div>
                </div>
            </div>

            <!-- Register Card -->
            <div class="col-md-4">
                <div class="card shadow auth-card h-100">
                    <div class="card-body text-center d-flex flex-column">
                        <h4 class="card-title mb-4">Register</h4>
                        <p class="card-text flex-grow-1">New to the system? Create a regular user account.</p>
                        <a href="/auth/register" class="btn btn-success">Register</a>
                    </div>
                </div>
            </div>

            <!-- Admin Registration Card -->
            <div class="col-md-4">
                <div class="card shadow auth-card h-100">
                    <div class="card-body text-center d-flex flex-column">
                        <h4 class="card-title mb-4">Create Admin</h4>
                        <p class="card-text flex-grow-1">Create an administrator account with full system access.</p>
                        <a href="/create-admin" class="btn btn-danger">Create Admin</a>
                    </div>
                </div>
            </div>
        </div>

        <!-- Test Account Info -->
        <div class="row justify-content-center mt-5">
            <div class="col-md-6">
                <div class="card shadow">
                    <div class="card-body">
                        <h5 class="card-title">Test Account</h5>
                        <p class="card-text">For testing purposes, you can use:</p>
                        <pre class="bg-light p-3 rounded">
Email: test@example.com
Password: password123</pre>
                    </div>
                </div>
            </div>
        </div>

        <!-- Back to Dashboard -->
        <div class="row justify-content-center mt-4 mb-5">
            <div class="col-md-6 text-center">
                <a href="/" class="text-decoration-none">← Back to Dashboard</a>
            </div>
        </div>
    </div>
</body>
</html>
